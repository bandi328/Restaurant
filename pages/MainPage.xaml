<Page x:Class="Restaurant.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Restaurant"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width=".3*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height=".3*"/>
            <RowDefinition/>
            <RowDefinition Height=".2*"/>
        </Grid.RowDefinitions>
        <Grid.Resources>
            <Style TargetType="Button">
                <Setter Property="Margin" Value="5 0"/>
                <Setter Property="Padding" Value="20 5"/>
            </Style>
        </Grid.Resources>
        <Label Content="Menu" FontSize="25" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Label Content="Cart" FontSize="25" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="2"/>
        <DataGrid Grid.Row="1" ItemsSource="{Binding MenuItems}" AutoGenerateColumns="False" CanUserReorderColumns="False" CanUserResizeColumns="False" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Width="*" Binding="{Binding name}" />
                <DataGridTextColumn Header="Price" Width=".3*" Binding="{Binding price}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Grid Grid.Row="1" Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height=".1*"/>
            </Grid.RowDefinitions>
            <DataGrid Grid.Row="0" ItemsSource="{}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Width="*"/>
                    <DataGridTextColumn Header="Price" Width=".3*"/>
                    <DataGridTextColumn Header="Count" Width=".3*"/>
                </DataGrid.Columns>
            </DataGrid>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" Grid.Row="1">
                <Label Content="Total:"/>
                <Label Content="0 $"/>
            </StackPanel>
        </Grid>
        <StackPanel Orientation="Vertical" Grid.Row="1" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button x:Name="add_BTN" Content="--&gt;"/>
            <Button x:Name="back_BTN" Content="&lt;--"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Grid.ColumnSpan="3" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button x:Name="order_BTN" Content="Order" Click="order_BTN_Click"/>
            <Button x:Name="clear_BTN" Content="Clear" Click="clear_BTN_Click"/>
        </StackPanel>
    </Grid>
</Page>
