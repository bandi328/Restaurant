<Window x:Class="Restaurant.windows.DataWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Restaurant.windows"
        mc:Ignorable="d"
        Title="Enter user info" Height="450" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.Resources>
            <Style TargetType="Label">
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="VerticalContentAlignment" Value="Center"/>
            </Style>
            <Style TargetType="Button">
                <Setter Property="Padding" Value="15 3"/>
            </Style>
        </Grid.Resources>
        <Label Content="Csapatnév:" FontWeight="Bold"/>
        <Label Content="Kategória:" Grid.Row="1" FontWeight="Bold"/>
        <Label Content="Csapattagok száma:" Grid.Row="2" FontWeight="Bold"/>
        <Label Content="Csapatvezető neve:" Grid.Row="3" FontWeight="Bold"/>
        <Label Content="Csapvez. e-mail:" Grid.Row="4" FontWeight="Bold"/>
        <!--<Label Content="Csapvez. szül. dátum:" Grid.Row="5"/>
        <TextBox Grid.Column="1" Height="30" Text="{Binding Team.TeamName}" IsEnabled="{Binding IsEnabledTXB}" VerticalContentAlignment="Center"/>
        <ComboBox Grid.Row="1" Grid.Column="1" Height="30" SelectedItem="{Binding Team.Category}" ItemsSource="{Binding Categories}"></ComboBox>
        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
            <RadioButton Content="2" VerticalAlignment="Center" VerticalContentAlignment="Center" IsChecked="{Binding NumOfMembers[0]}" GroupName="MemberCountRadio"/>
            <RadioButton Content="3" VerticalAlignment="Center" VerticalContentAlignment="Center" IsChecked="{Binding NumOfMembers[1]}" Margin="20 0 0 0" GroupName="MemberCountRadio"/>
        </StackPanel>
        <TextBox Grid.Row="3" Grid.Column="1" Height="30" Text="{Binding Team.TeamLeader}"/>
        <TextBox Grid.Row="4" Grid.Column="1" Height="30" Text="{Binding Team.TeamLeaderEmail}"/>
        <DatePicker Grid.Row="5" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Left" SelectedDate="{Binding SelectedDate}"/>
        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Content="Mentés" x:Name="save_BTN"/>
            <Button Content="Mégsem" x:Name="cancel_BTN" Margin="20 0 0 0"/>
        </StackPanel>-->
    </Grid>
</Window>
